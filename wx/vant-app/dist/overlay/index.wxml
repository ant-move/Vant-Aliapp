<van-transition
  show="{{ show }}"
  custom-class="van-overlay"
  custom-style="z-index: {{ zIndex }}; {{ customStyle }}"
  duration="{{ duration }}"
  bind:click="onClick"
  catch:touchmove="noop"
  inited='{{inited}}' 
  display='{{display}}'
>
  <slot></slot>
</van-transition>
